# Configurazione Camera
# IP del dispositivo con camera (es. 192.168.1.17)
CAMERA_IP=192.168.1.17

# Porta camera (default: 8080 per IP Webcam, 4747 per DroidCam)
CAMERA_PORT=8080

# App camera da usare: "droidcam" oppure "ipwebcam"
# Se non specificato, auto-rileva in base alla porta (8080 = IP Webcam)
CAMERA_APP=ipwebcam

# API Key di OpenRouter
# Ottienila da: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-...

# Modello OpenRouter da utilizzare
# Modelli consigliati con supporto vision:
# - openai/gpt-4o
# - openai/gpt-4-vision-preview
# - google/gemini-pro-vision
# - nvidia/nemotron-nano-12b-v2-vl:free (gratuito)
OPENROUTER_MODEL=nvidia/nemotron-nano-12b-v2-vl:free

# ── Sensibilità rilevamento ROI ────────────────────────────────────────────────
# Sensibilità del rilevamento dello schermo (1.0 - 10.0)
#   1-3  = molto sensibile (rileva ROI piccole, accetta qualità bassa)
#   4-6  = sensibilità media (default: 5.0)
#   7-10 = poco sensibile (richiede ROI grandi, qualità alta)
# Questo parametro controlla automaticamente tutti gli aspetti del rilevamento
ROI_SENSITIVITY=5.0

